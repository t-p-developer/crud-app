import Container from '@mui/material/Container';
import * as React from 'react';

export const LoginPage = () => (
  <Container maxWidth='xl' sx={{ width: '100%', margin: 0, padding: 0, height: '100vh' }}>
    LoginPage
  </Container>
);
